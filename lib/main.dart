import 'package:flutter/material.dart';
import 'package:just_bus_tracker/screens/student/login_screen.dart'; // تأكد من صحة المسار

void main() {
  runApp(const MaterialApp(
    debugShowCheckedModeBanner: false,
    home: LoginScreen(), // عرض شاشتك الجديدة
  ));
}